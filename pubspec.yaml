name: beam_reserve
description: "BeamReserve - Gestión y reservación de proyectores"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.4
  flutter_dotenv: ^5.2.1
  web_socket_channel: ^3.0.2
  provider: ^6.1.4
  intl: ^0.20.2
  animate_do: ^3.3.4
  google_fonts: ^6.2.1
  cached_network_image: ^3.4.1
  font_awesome_flutter: ^10.8.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/
